---
layout: /layouts/base.njk
---
<div class="video-item--wrapper">
  {% for video in collections.videos | categoryFilter(slug) | reverse %}
    <div class="video-item">
      <div class="video-item--proportional">
        {{ video.data.code | safe }}
      </div>
      <h3 class="video-item--title">{{ video.data.title }} <span>{{ video.data.subtitle }}</span></h3>
    </div>
  {% endfor %}
</div>